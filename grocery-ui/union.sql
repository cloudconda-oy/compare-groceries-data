-- CREATE TABLE master (
-- 	title VARCHAR(255),
-- 	quantity VARCHAR(255),
-- 	actual_price_in_rs VARCHAR(255),
-- 	discounted_price_in_rs VARCHAR(255),
-- 	stock VARCHAR(255),
-- 	category VARCHAR(255),
-- 	store VARCHAR(255)
-- )

-- SELECT * FROM master

-- INSERT INTO master(title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store)
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM alfatah UNION
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM aljadeed UNION
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM binhashim UNION 
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM carrefour UNION 
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM chaseup UNION 
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM freshbasket UNION 
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM imtiaz UNION
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM keryana UNION
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM naheed UNION
-- SELECT title, quantity, actual_price_in_rs, discounted_price_in_rs, stock, category, store FROM qne

-- SELECT * FROM master